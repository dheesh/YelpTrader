var config = {
    credentials:{
        consumer_key:"consumer key goes here",
        consumer_secret:"consumer secret goes here",
        token:"token goes heree",
        token_secret:"token secret goes here"
    }
};

module.exports = config;